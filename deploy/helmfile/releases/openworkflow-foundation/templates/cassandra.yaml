# Licensed to the Apache Software Foundation (ASF) under one or more contributor license
# agreements. See the NOTICE file distributed with this work for additional information regarding
# copyright ownership. The ASF licenses this file to You under the Apache License, Version 2.0.
{{- if .Values.cassandra.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: cassandra
spec:
  selector: {app: cassandra}
  ports:
    - {name: cql, port: 9042, targetPort: cql}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: cassandra
spec:
  serviceName: cassandra
  replicas: 1
  selector:
    matchLabels: {app: cassandra}
  template:
    metadata:
      labels: {app: cassandra}
    spec:
      containers:
        - name: cassandra
          image: {{ .Values.images.cassandra.repository }}{{ if .Values.images.cassandra.digest }}@{{ .Values.images.cassandra.digest }}{{ else }}:{{ .Values.images.cassandra.tag }}{{ end }}
          imagePullPolicy: IfNotPresent
          env:
            - {name: CASSANDRA_CLUSTER_NAME, value: openworkflow}
            - {name: CASSANDRA_DC, value: datacenter1}
            - {name: CASSANDRA_ENDPOINT_SNITCH, value: GossipingPropertyFileSnitch}
            - {name: MAX_HEAP_SIZE, value: 512M}
            - {name: HEAP_NEWSIZE, value: 100M}
          ports:
            - {name: cql, containerPort: 9042}
          readinessProbe:
            exec: {command: ["cqlsh", "127.0.0.1", "9042", "-e", "SELECT release_version FROM system.local"]}
            initialDelaySeconds: 20
            timeoutSeconds: 10
            periodSeconds: 5
          resources:
            requests: {cpu: 100m, memory: 768Mi}
            limits: {cpu: "2", memory: 1Gi}
{{- end }}
